[FORMAT]
format = mp4

[VIDEO]
encoder = libx264
pixel_format = yuv420p
options = preset=ultrafast / crf=1.0 / g=1 / profile=high / level=5.2

[AUDIO]
encoder = ac3
sample_format = fltp
sample_rate = 48000
options = b=640000