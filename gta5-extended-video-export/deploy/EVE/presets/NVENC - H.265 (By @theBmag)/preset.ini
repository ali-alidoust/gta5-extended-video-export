[FORMAT]
format = mp4
extension = mp4
options = movflags=+faststart

[VIDEO]
encoder = hevc_nvenc
pixel_format = nv12
options = global_quality=10 / preset=slow

[AUDIO]
encoder = aac
sample_format = fltp
options = ar=48000 / b=384k