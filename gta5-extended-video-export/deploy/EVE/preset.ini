[FORMAT]
format = mp4
extension = mp4
options = movflags=+faststart

[VIDEO]
encoder = libx264
pixel_format = yuv420p
options = crf=2 / bf=2 / flags=+cgop

[AUDIO]
encoder = aac
sample_format = fltp
options = ar=48000 / b=384k